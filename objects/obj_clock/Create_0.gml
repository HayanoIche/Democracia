// Iniciar relógio
clock_init();

// Opcional: Vc consegue definir uma quantidade customizada (pra gnt testar, em segundos reais)
// clock_set_day_length(360); // Dias de 6 minutos em vez de 4,5 minutos