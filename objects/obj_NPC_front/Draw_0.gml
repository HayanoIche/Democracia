/// @description
if (sprite_exists(sprite_index))
{
	draw_sprite_ext(sprite_index,
		image_index,
		obj_NPC.x,
		obj_NPC.y,
		image_xscale,
		image_yscale,
		image_angle,
		image_blend,
		image_alpha
	);
}

